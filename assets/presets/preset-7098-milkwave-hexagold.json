{
  "version": 6,
  "metadata": {
    "version": 6,
    "name": "HexaGold",
    "author": "aiekick + IkeC",
    "source": "aiekick + IkeC - HexaGold.milk",
    "importedFrom": "Milkwave",
    "createdAt": "2026-03-20T00:49:40.118Z",
    "updatedAt": "2026-03-20T00:49:40.118Z",
    "category": "Atmosphere & Terrain",
    "compatibility": {
      "minVersion": "1.4.0"
    },
    "milkwave": {
      "format": "milkwave-ir",
      "version": 1,
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-comp",
        "custom-texture-slots"
      ],
      "reasons": [],
      "translation": {
        "pipeline": "milkwave-offline-v1",
        "runtimePatchRecommended": true,
        "generatedPasses": [
          "comp"
        ]
      }
    },
    "activeEngineId": "engine-radial-core",
    "activeModeId": "mode-cosmic",
    "intendedMusicStyle": "Electronic",
    "visualIntentTags": [
      "imported",
      "milkwave"
    ],
    "colorChemistry": [
      "analog",
      "balanced"
    ],
    "defaultTransition": {
      "durationMs": 600,
      "curve": "easeInOut"
    }
  },
  "scenes": [
    {
      "id": "scene-1",
      "scene_id": "scene-1",
      "name": "Main",
      "intent": "ambient",
      "duration": 0,
      "transition_in": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "transition_out": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "trigger": {
        "type": "manual"
      },
      "assigned_layers": {
        "core": [],
        "support": [
          "layer-milkwave"
        ],
        "atmosphere": []
      },
      "layers": [
        {
          "id": "layer-milkwave",
          "name": "Milkwave",
          "role": "support",
          "enabled": true,
          "opacity": 1,
          "blendMode": "screen",
          "transform": {
            "x": 0,
            "y": 0,
            "scale": 1,
            "rotation": 0
          },
          "params": {
            "opacity": 1,
            "enabled": true,
            "blendMode": "screen",
            "transform": {
              "x": 0,
              "y": 0,
              "scale": 1,
              "rotation": 0
            },
            "zoom": 1,
            "rotation": 0,
            "warp": 0.01,
            "decay": 0.95,
            "gamma": 1,
            "bassSensitivity": 1,
            "midSensitivity": 1,
            "trebSensitivity": 1
          }
        }
      ]
    }
  ],
  "activeSceneId": "scene-1",
  "roleWeights": {
    "core": 1,
    "support": 1,
    "atmosphere": 1
  },
  "tempoSync": {
    "bpm": 120,
    "source": "manual"
  },
  "modulations": [],
  "macros": [],
  "_shaderData": {
    "warp": "",
    "comp": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\n// Custom sampler stubs (mapped to noise texture)\n#define sampler_stoy_cubemap_uffizi_blurred sampler_noise_lq\n// aiekick + IkeC - HexaGold / https://www.shadertoy.com/view / 7.0lV3Wd\n// Transpiled to HLSL using Milkwave\n#define tx sin(uTime)*0.5+1\n// 0.5 <= tx <= 1.5\n#define iChannel0 sampler_stoy_cubemap_uffizi_blurred\nsampler2D iChannel0;\n// CONV: adding helper functions\nfloat mod_conv(float x, float y) { return x - y * floor(x / y); }\nvec2 mod_conv(vec2 x, vec2 y) { return x - y * floor(x / y); }\nvec3 mod_conv(vec3 x, vec3 y) { return x - y * floor(x / y); }\nvec4 mod_conv(vec4 x, vec4 y) { return x - y * floor(x / y); }\n#define THUMBNAIL_ANGLE_OFFSET 0.35\n#define LOOPS 200\nfloat sdHexPrism( vec3 p, vec2 h )\n{\nvec3 q = abs(p);\nvec3 k = vec3(-0.8660254, 0.5, 0.57735);\np = abs(p);\np.xy -= 2.0*min(dot(k.xy, p.xy), 0.0)*k.xy;\nvec2 d = vec2(\nlength(p.xy - vec2(clamp(p.x, -k.z*h.x, k.z*h.x), h.x))*sign(p.y - h.x),\np.z-h.y );\nreturn min(max(d.x,d.y),0.0) + length(max(d,0.0));\n}\n#define ox 1.3\n#define oz 1.5\n#define hex_size vec2(0.5 - p.y * 0.1, 10)\nvoid common_map(vec3 p, out float df0, out float df1)\n{\ndf0 = p.y - 1.0 + sin(length(p.xz) * 0.8 - uTime);\nvec3 q0 = p;\nq0.x = mod_conv(q0.x - ox, ox + ox) - ox;\nq0.z = mod_conv(q0.z - oz * 0.5, oz) - oz * 0.5;\nfloat hex0 = sdHexPrism(q0.xzy, hex_size) - 0.2;\nvec3 q1_c = p;\nq1_c.x = mod_conv(q1_c.x, ox + ox) - ox;\nq1_c.z = mod_conv(q1_c.z, oz) - oz * 0.5;\nfloat hex1 = sdHexPrism(q1_c.xzy, hex_size) - 0.2;\ndf1 = min(hex0, hex1);\n}\nfloat smin( float a, float b, float k )\n{\nfloat h = clamp( 0.5 + 0.5*(b-a)/k, 0.0, 1.0 );\nreturn mix( b, a, h ) - k*h*(1.0-h);\n}\nfloat smax(float a, float b, float k)\n{\nreturn smin(a, b, -k);\n}\nfloat map(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nreturn smax(df0, df1, 0.1);\n}\nfloat mat(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nfloat res = 0;\nif (df0 > df1) {\nres = 1;\n}\nelse {\nres = 0;\n}\nreturn res;\n}\nvec3 getNormal(vec3 p, float prec)\n{\nvec3 e = vec3(prec, 0.0 , 0.0 );\nreturn normalize(vec3(\nmap(p+e)-map(p-e),\nmap(p+e.yxz)-map(p-e.yxz),\nmap(p+e.zyx)-map(p-e.zyx)));\n}\nfloat getAmbiantOcclusion(vec3 p, vec3 n, float k)\n{\nconst float aoStep = 0.1;\nfloat occl = 0.;\nfor(int i = min(uFrame,0); i < 6; ++i)\n{\nfloat diff = float(i)*aoStep;\nfloat d = map(p + n*diff);\noccl += (diff - d) * pow(2., float(-i));\n}\nreturn min(1., 1. - k*occl);\n}\nfloat getShadow(vec3 ro, vec3 rd, float minD, float maxD, float k)\n{\nfloat res = 1.0;\nfloat d = minD;\nfloat s = 0.;\nfor(int i = min(uFrame,0); i < 20; ++i)\n{\ns = map(ro + rd * d);\nif( abs(s)<d*d * 1.0e - 5.0 ) {\nres = 0;\n// return 0.0;\n}\nelse {\nres = min( res, k * s / d );\nd += s;\n// CONV: break unsupported, see Milkwave manual\nif(d >= maxD) i=20;\n}\n}\nreturn res;\n}\nvec3 cam(vec2 uv_conv,  vec3 ro, vec3 cv, float fov)\n{\nvec3 cu = normalize(vec3( 0.0 , 1.0 , 0.0 ));\nvec3 z = normalize(cv-ro);\nvec3 x = normalize(cross(cu,z));\nvec3 y = cross(z,x);\nreturn normalize(z + fov*uv_conv.x*x + fov*uv_conv.y*y);\n}\nvec3 hsv2rgb_smooth( in vec3 c )\n{\nvec3 rgb = clamp( abs(mod_conv(c.x*6.0+vec3(0.0,4.0,2.0),6.0)-3.0)-1.0, 0.0, 1.0 );\nrgb = rgb*rgb*(3.0-2.0*rgb);\n// cubic smoothing\n#ifdef bass_smooth\nfloat fac = (int)vis_version > 2 ? 1.0 * vis_intensity+vis_shift : (0.65 + smoothstep( 0.0 , 2.0 ,(int)vis_version == 1 ? bass_smooth : audioLowSmooth)*0.5)*vis_intensity+vis_shift;\n#else\nfloat fac = 0.8 + clamp(audioLowSmooth,0.6,1.2)*0.2;\n#endif\nreturn c.z * fac * mix( vec3(1.0, 1.0, 1.0), rgb, c.y);\n}\nelse {\ns = map(ro + rd * d);\nd += s * 0.5;\n}\n}\nif (d < md)\n{\n// surface point\nvec3 p = ro + rd * d;\n// surface normal, precision of 0.1 for remove some aliasing\nvec3 n = getNormal(p, 0.1);\n// light pos\nvec3 lp = vec3( 0.0 , 5.0 , 0.0 );\n// light dir\nvec3 ld = normalize(lp - p);\n// diffuse, ambiant occlusion, shadow, specular\nfloat diff = pow(dot(n, ld) * .5 + .5,2.0);\nfloat ao = getAmbiantOcclusion(p, n, 40.0);\nfloat sha = clamp(getShadow(p, ld, 0.01, 150.0, 5.0), 0. ,0.9);\nfloat spe = pow(max(dot(-rd, reflect(-ld, n)), 0.0), 32.0);\nif (mat(p) < 0.5)\n// hexa sides\n{\n// smooth hsv\nvec3 base = hsv2rgb_smooth(vec3(atan(p.x,p.z)/3.14159*0.5 - uTime * 0.1, 0.8, 0.8));\n// vary base color according to ao\ncol = mix(base, vec3( 1.0 , 1.0 , 1.0), ao) * 0.5;\n}\nelse\n// hexa face\n{\n// reflected gold\ncol = vec3(1.0, 0.85, 0.0) * texture(iChannel0, reflect(rd, n)).rgb;\n}\n// final brdf\ncol += diff * sha * 0.5 + spe;\n// clamp for avoid overlight\ncol = clamp(col, 0., 1.);\n}\n// distance fog\ncol *= exp(1.0-d*d*0.001);\n// final color\nfragColor = vec4(col, 1.0 );\nret = fragColor;\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  vec4 fragColor = 0;\n// CONV: Center on screen, then try some uAspect correction\nuv = (uv * 2.0) - 1.0;\nuv *= uAspect.xy;\n// CONV: Adjust this to flip the output (±uv.x, ±uv.y)\nuv = vec2(uv.x, -uv.y);\nvec2 fragCoord  = uv;\nvec2 si = uv.xy;\n// central uv\nvec2 uvc = uv;\n// (2.*fragCoord.xy-si)/si.y;\n// classice turning table camera\nfloat a = uTime * 0.1 + THUMBNAIL_ANGLE_OFFSET;\nvec3 ro = vec3(cos(a), 0.0, sin(a)) * 20.0;\nro.y = 20.0;\nvec3 rd = cam(uvc, ro, vec3( 0.0 , 0.0 , 0.0 ), 0.4);\nvec3 col = vec3(0.1, 0.1, 0.1);\n// log raymarching\nfloat s = 1., d = 0., md = 100.;\nfor (int i = min(uFrame,0); i < LOOPS; i++)\n{\n// CONV: break unsupported, see Milkwave manual\nif (d*d/s>1e8 || d > 70.) {\ni=LOOPS;\n  fragColor = vec4(ret, 1.0);\n}",
    "perFrameCode": [],
    "perFrameInitCode": [],
    "perPixelCode": [],
    "waves": [],
    "shapes": [],
    "originalParameters": {},
    "translation": {
      "pipeline": "milkwave-offline-v1",
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-comp",
        "custom-texture-slots"
      ],
      "runtimePatchRecommended": true,
      "passes": {
        "warp": {
          "kind": "warp",
          "requested": false,
          "generated": false,
          "source": "",
          "backend": "milkwave-direct-v2",
          "normalized": null,
          "warnings": [],
          "errors": [],
          "diagnostics": null
        },
        "comp": {
          "kind": "comp",
          "requested": true,
          "generated": true,
          "source": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\n// Custom sampler stubs (mapped to noise texture)\n#define sampler_stoy_cubemap_uffizi_blurred sampler_noise_lq\n// aiekick + IkeC - HexaGold / https://www.shadertoy.com/view / 7.0lV3Wd\n// Transpiled to HLSL using Milkwave\n#define tx sin(uTime)*0.5+1\n// 0.5 <= tx <= 1.5\n#define iChannel0 sampler_stoy_cubemap_uffizi_blurred\nsampler2D iChannel0;\n// CONV: adding helper functions\nfloat mod_conv(float x, float y) { return x - y * floor(x / y); }\nvec2 mod_conv(vec2 x, vec2 y) { return x - y * floor(x / y); }\nvec3 mod_conv(vec3 x, vec3 y) { return x - y * floor(x / y); }\nvec4 mod_conv(vec4 x, vec4 y) { return x - y * floor(x / y); }\n#define THUMBNAIL_ANGLE_OFFSET 0.35\n#define LOOPS 200\nfloat sdHexPrism( vec3 p, vec2 h )\n{\nvec3 q = abs(p);\nvec3 k = vec3(-0.8660254, 0.5, 0.57735);\np = abs(p);\np.xy -= 2.0*min(dot(k.xy, p.xy), 0.0)*k.xy;\nvec2 d = vec2(\nlength(p.xy - vec2(clamp(p.x, -k.z*h.x, k.z*h.x), h.x))*sign(p.y - h.x),\np.z-h.y );\nreturn min(max(d.x,d.y),0.0) + length(max(d,0.0));\n}\n#define ox 1.3\n#define oz 1.5\n#define hex_size vec2(0.5 - p.y * 0.1, 10)\nvoid common_map(vec3 p, out float df0, out float df1)\n{\ndf0 = p.y - 1.0 + sin(length(p.xz) * 0.8 - uTime);\nvec3 q0 = p;\nq0.x = mod_conv(q0.x - ox, ox + ox) - ox;\nq0.z = mod_conv(q0.z - oz * 0.5, oz) - oz * 0.5;\nfloat hex0 = sdHexPrism(q0.xzy, hex_size) - 0.2;\nvec3 q1_c = p;\nq1_c.x = mod_conv(q1_c.x, ox + ox) - ox;\nq1_c.z = mod_conv(q1_c.z, oz) - oz * 0.5;\nfloat hex1 = sdHexPrism(q1_c.xzy, hex_size) - 0.2;\ndf1 = min(hex0, hex1);\n}\nfloat smin( float a, float b, float k )\n{\nfloat h = clamp( 0.5 + 0.5*(b-a)/k, 0.0, 1.0 );\nreturn mix( b, a, h ) - k*h*(1.0-h);\n}\nfloat smax(float a, float b, float k)\n{\nreturn smin(a, b, -k);\n}\nfloat map(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nreturn smax(df0, df1, 0.1);\n}\nfloat mat(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nfloat res = 0;\nif (df0 > df1) {\nres = 1;\n}\nelse {\nres = 0;\n}\nreturn res;\n}\nvec3 getNormal(vec3 p, float prec)\n{\nvec3 e = vec3(prec, 0.0 , 0.0 );\nreturn normalize(vec3(\nmap(p+e)-map(p-e),\nmap(p+e.yxz)-map(p-e.yxz),\nmap(p+e.zyx)-map(p-e.zyx)));\n}\nfloat getAmbiantOcclusion(vec3 p, vec3 n, float k)\n{\nconst float aoStep = 0.1;\nfloat occl = 0.;\nfor(int i = min(uFrame,0); i < 6; ++i)\n{\nfloat diff = float(i)*aoStep;\nfloat d = map(p + n*diff);\noccl += (diff - d) * pow(2., float(-i));\n}\nreturn min(1., 1. - k*occl);\n}\nfloat getShadow(vec3 ro, vec3 rd, float minD, float maxD, float k)\n{\nfloat res = 1.0;\nfloat d = minD;\nfloat s = 0.;\nfor(int i = min(uFrame,0); i < 20; ++i)\n{\ns = map(ro + rd * d);\nif( abs(s)<d*d * 1.0e - 5.0 ) {\nres = 0;\n// return 0.0;\n}\nelse {\nres = min( res, k * s / d );\nd += s;\n// CONV: break unsupported, see Milkwave manual\nif(d >= maxD) i=20;\n}\n}\nreturn res;\n}\nvec3 cam(vec2 uv_conv,  vec3 ro, vec3 cv, float fov)\n{\nvec3 cu = normalize(vec3( 0.0 , 1.0 , 0.0 ));\nvec3 z = normalize(cv-ro);\nvec3 x = normalize(cross(cu,z));\nvec3 y = cross(z,x);\nreturn normalize(z + fov*uv_conv.x*x + fov*uv_conv.y*y);\n}\nvec3 hsv2rgb_smooth( in vec3 c )\n{\nvec3 rgb = clamp( abs(mod_conv(c.x*6.0+vec3(0.0,4.0,2.0),6.0)-3.0)-1.0, 0.0, 1.0 );\nrgb = rgb*rgb*(3.0-2.0*rgb);\n// cubic smoothing\n#ifdef bass_smooth\nfloat fac = (int)vis_version > 2 ? 1.0 * vis_intensity+vis_shift : (0.65 + smoothstep( 0.0 , 2.0 ,(int)vis_version == 1 ? bass_smooth : audioLowSmooth)*0.5)*vis_intensity+vis_shift;\n#else\nfloat fac = 0.8 + clamp(audioLowSmooth,0.6,1.2)*0.2;\n#endif\nreturn c.z * fac * mix( vec3(1.0, 1.0, 1.0), rgb, c.y);\n}\nelse {\ns = map(ro + rd * d);\nd += s * 0.5;\n}\n}\nif (d < md)\n{\n// surface point\nvec3 p = ro + rd * d;\n// surface normal, precision of 0.1 for remove some aliasing\nvec3 n = getNormal(p, 0.1);\n// light pos\nvec3 lp = vec3( 0.0 , 5.0 , 0.0 );\n// light dir\nvec3 ld = normalize(lp - p);\n// diffuse, ambiant occlusion, shadow, specular\nfloat diff = pow(dot(n, ld) * .5 + .5,2.0);\nfloat ao = getAmbiantOcclusion(p, n, 40.0);\nfloat sha = clamp(getShadow(p, ld, 0.01, 150.0, 5.0), 0. ,0.9);\nfloat spe = pow(max(dot(-rd, reflect(-ld, n)), 0.0), 32.0);\nif (mat(p) < 0.5)\n// hexa sides\n{\n// smooth hsv\nvec3 base = hsv2rgb_smooth(vec3(atan(p.x,p.z)/3.14159*0.5 - uTime * 0.1, 0.8, 0.8));\n// vary base color according to ao\ncol = mix(base, vec3( 1.0 , 1.0 , 1.0), ao) * 0.5;\n}\nelse\n// hexa face\n{\n// reflected gold\ncol = vec3(1.0, 0.85, 0.0) * texture(iChannel0, reflect(rd, n)).rgb;\n}\n// final brdf\ncol += diff * sha * 0.5 + spe;\n// clamp for avoid overlight\ncol = clamp(col, 0., 1.);\n}\n// distance fog\ncol *= exp(1.0-d*d*0.001);\n// final color\nfragColor = vec4(col, 1.0 );\nret = fragColor;\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  vec4 fragColor = 0;\n// CONV: Center on screen, then try some uAspect correction\nuv = (uv * 2.0) - 1.0;\nuv *= uAspect.xy;\n// CONV: Adjust this to flip the output (±uv.x, ±uv.y)\nuv = vec2(uv.x, -uv.y);\nvec2 fragCoord  = uv;\nvec2 si = uv.xy;\n// central uv\nvec2 uvc = uv;\n// (2.*fragCoord.xy-si)/si.y;\n// classice turning table camera\nfloat a = uTime * 0.1 + THUMBNAIL_ANGLE_OFFSET;\nvec3 ro = vec3(cos(a), 0.0, sin(a)) * 20.0;\nro.y = 20.0;\nvec3 rd = cam(uvc, ro, vec3( 0.0 , 0.0 , 0.0 ), 0.4);\nvec3 col = vec3(0.1, 0.1, 0.1);\n// log raymarching\nfloat s = 1., d = 0., md = 100.;\nfor (int i = min(uFrame,0); i < LOOPS; i++)\n{\n// CONV: break unsupported, see Milkwave manual\nif (d*d/s>1e8 || d > 70.) {\ni=LOOPS;\n  fragColor = vec4(ret, 1.0);\n}",
          "backend": "milkwave-direct-v2",
          "normalized": {
            "preludeLineCount": 174,
            "bodyLineCount": 24,
            "helperLineCount": 6,
            "hasShaderBodyBlock": true,
            "source": "// aiekick + IkeC - HexaGold / https://www.shadertoy.com/view/7lV3Wd\n// Transpiled to HLSL using Milkwave\n#define tx sin(time)*0.5+1\n// 0.5 <= tx <= 1.5\n#define iChannel0 sampler_stoy_cubemap_uffizi_blurred\nsampler2D iChannel0;\n// CONV: adding helper functions\nfloat mod_conv(float x, float y) { return x - y * floor(x / y); }\nfloat2 mod_conv(float2 x, float2 y) { return x - y * floor(x / y); }\nfloat3 mod_conv(float3 x, float3 y) { return x - y * floor(x / y); }\nfloat4 mod_conv(float4 x, float4 y) { return x - y * floor(x / y); }\n#define THUMBNAIL_ANGLE_OFFSET 0.35\n#define LOOPS 200\nfloat sdHexPrism( float3 p, float2 h )\n{\nfloat3 q = abs(p);\nfloat3 k = float3(-0.8660254, 0.5, 0.57735);\np = abs(p);\np.xy -= 2.0*min(dot(k.xy, p.xy), 0.0)*k.xy;\nfloat2 d = float2(\nlength(p.xy - float2(clamp(p.x, -k.z*h.x, k.z*h.x), h.x))*sign(p.y - h.x),\np.z-h.y );\nreturn min(max(d.x,d.y),0.0) + length(max(d,0.0));\n}\n#define ox 1.3\n#define oz 1.5\n#define hex_size float2(0.5 - p.y * 0.1, 10)\nvoid common_map(float3 p, out float df0, out float df1)\n{\ndf0 = p.y - 1.0 + sin(length(p.xz) * 0.8 - time);\nfloat3 q0 = p;\nq0.x = mod_conv(q0.x - ox, ox + ox) - ox;\nq0.z = mod_conv(q0.z - oz * 0.5, oz) - oz * 0.5;\nfloat hex0 = sdHexPrism(q0.xzy, hex_size) - 0.2;\nfloat3 q1_c = p;\nq1_c.x = mod_conv(q1_c.x, ox + ox) - ox;\nq1_c.z = mod_conv(q1_c.z, oz) - oz * 0.5;\nfloat hex1 = sdHexPrism(q1_c.xzy, hex_size) - 0.2;\ndf1 = min(hex0, hex1);\n}\nfloat smin( float a, float b, float k )\n{\nfloat h = clamp( 0.5 + 0.5*(b-a)/k, 0.0, 1.0 );\nreturn lerp( b, a, h ) - k*h*(1.0-h);\n}\nfloat smax(float a, float b, float k)\n{\nreturn smin(a, b, -k);\n}\nfloat map(float3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nreturn smax(df0, df1, 0.1);\n}\nfloat mat(float3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nfloat res = 0;\nif (df0 > df1) {\nres = 1;\n}\nelse {\nres = 0;\n}\nreturn res;\n}\nfloat3 getNormal(float3 p, float prec)\n{\nfloat3 e = float3(prec, 0, 0);\nreturn normalize(float3(\nmap(p+e)-map(p-e),\nmap(p+e.yxz)-map(p-e.yxz),\nmap(p+e.zyx)-map(p-e.zyx)));\n}\nfloat getAmbiantOcclusion(float3 p, float3 n, float k)\n{\nconst float aoStep = 0.1;\nfloat occl = 0.;\nfor(int i = min(frame,0); i < 6; ++i)\n{\nfloat diff = float(i)*aoStep;\nfloat d = map(p + n*diff);\noccl += (diff - d) * pow(2., float(-i));\n}\nreturn min(1., 1. - k*occl);\n}\nfloat getShadow(float3 ro, float3 rd, float minD, float maxD, float k)\n{\nfloat res = 1.0;\nfloat d = minD;\nfloat s = 0.;\nfor(int i = min(frame,0); i < 20; ++i)\n{\ns = map(ro + rd * d);\nif( abs(s)<d*d*1e-5 ) {\nres = 0;\n// return 0.0;\n}\nelse {\nres = min( res, k * s / d );\nd += s;\n// CONV: break unsupported, see Milkwave manual\nif(d >= maxD) i=20;\n}\n}\nreturn res;\n}\nfloat3 cam(float2 uv_conv,  float3 ro, float3 cv, float fov)\n{\nfloat3 cu = normalize(float3(0,1,0));\nfloat3 z = normalize(cv-ro);\nfloat3 x = normalize(cross(cu,z));\nfloat3 y = cross(z,x);\nreturn normalize(z + fov*uv_conv.x*x + fov*uv_conv.y*y);\n}\nfloat3 hsv2rgb_smooth( in float3 c )\n{\nfloat3 rgb = clamp( abs(mod_conv(c.x*6.0+float3(0.0,4.0,2.0),6.0)-3.0)-1.0, 0.0, 1.0 );\nrgb = rgb*rgb*(3.0-2.0*rgb);\n// cubic smoothing\n#ifdef bass_smooth\nfloat fac = (int)vis_version > 2 ? 1*vis_intensity+vis_shift : (0.65 + smoothstep(0,2,(int)vis_version == 1 ? bass_smooth : bass_att)*0.5)*vis_intensity+vis_shift;\n#else\nfloat fac = 0.8 + clamp(bass_att,0.6,1.2)*0.2;\n#endif\nreturn c.z * fac * lerp( float3(1.0, 1.0, 1.0), rgb, c.y);\n}\nelse {\ns = map(ro + rd * d);\nd += s * 0.5;\n}\n}\nif (d < md)\n{\n// surface point\nfloat3 p = ro + rd * d;\n// surface normal, precision of 0.1 for remove some aliasing\nfloat3 n = getNormal(p, 0.1);\n// light pos\nfloat3 lp = float3(0,5,0);\n// light dir\nfloat3 ld = normalize(lp - p);\n// diffuse, ambiant occlusion, shadow, specular\nfloat diff = pow(dot(n, ld) * .5 + .5,2.0);\nfloat ao = getAmbiantOcclusion(p, n, 40.0);\nfloat sha = clamp(getShadow(p, ld, 0.01, 150.0, 5.0), 0. ,0.9);\nfloat spe = pow(max(dot(-rd, reflect(-ld, n)), 0.0), 32.0);\nif (mat(p) < 0.5)\n// hexa sides\n{\n// smooth hsv\nfloat3 base = hsv2rgb_smooth(float3(atan2(p.x,p.z)/3.14159*0.5 - time * 0.1, 0.8, 0.8));\n// vary base color according to ao\ncol = lerp(base, float3(1, 1, 1), ao) * 0.5;\n}\nelse\n// hexa face\n{\n// reflected gold\ncol = float3(1.0, 0.85, 0.0) * tex2D(iChannel0, reflect(rd, n)).rgb;\n}\n// final brdf\ncol += diff * sha * 0.5 + spe;\n// clamp for avoid overlight\ncol = clamp(col, 0., 1.);\n}\n// distance fog\ncol *= exp(1.0-d*d*0.001);\n// final color\nfragColor = float4(col,1);\nret = fragColor;\n}\nshader_body {\nfloat4 fragColor = 0;\n// CONV: Center on screen, then try some aspect correction\nuv = (uv*2) - 1;\nuv *= aspect.xy;\n// CONV: Adjust this to flip the output (±uv.x, ±uv.y)\nuv = float2(uv.x, -uv.y);\nfloat2 fragCoord  = uv;\nfloat2 si = uv.xy;\n// central uv\nfloat2 uvc = uv;\n// (2.*fragCoord.xy-si)/si.y;\n// classice turning table camera\nfloat a = time * 0.1 + THUMBNAIL_ANGLE_OFFSET;\nfloat3 ro = float3(cos(a), 0.0, sin(a)) * 20.0;\nro.y = 20.0;\nfloat3 rd = cam(uvc, ro, float3(0, 0, 0), 0.4);\nfloat3 col = float3(0.1, 0.1, 0.1);\n// log raymarching\nfloat s = 1., d = 0., md = 100.;\nfor (int i = min(frame,0); i < LOOPS; i++)\n{\n// CONV: break unsupported, see Milkwave manual\nif (d*d/s>1e8 || d > 70.) {\ni=LOOPS;\n}",
            "dialectSource": "// aiekick + IkeC - HexaGold / https://www.shadertoy.com/view / 7.0lV3Wd\n// Transpiled to HLSL using Milkwave\n#define tx sin(uTime)*0.5+1\n// 0.5 <= tx <= 1.5\n#define iChannel0 sampler_stoy_cubemap_uffizi_blurred\nsampler2D iChannel0;\n// CONV: adding helper functions\nfloat mod_conv(float x, float y) { return x - y * floor(x / y); }\nvec2 mod_conv(vec2 x, vec2 y) { return x - y * floor(x / y); }\nvec3 mod_conv(vec3 x, vec3 y) { return x - y * floor(x / y); }\nvec4 mod_conv(vec4 x, vec4 y) { return x - y * floor(x / y); }\n#define THUMBNAIL_ANGLE_OFFSET 0.35\n#define LOOPS 200\nfloat sdHexPrism( vec3 p, vec2 h )\n{\nvec3 q = abs(p);\nvec3 k = vec3(-0.8660254, 0.5, 0.57735);\np = abs(p);\np.xy -= 2.0*min(dot(k.xy, p.xy), 0.0)*k.xy;\nvec2 d = vec2(\nlength(p.xy - vec2(clamp(p.x, -k.z*h.x, k.z*h.x), h.x))*sign(p.y - h.x),\np.z-h.y );\nreturn min(max(d.x,d.y),0.0) + length(max(d,0.0));\n}\n#define ox 1.3\n#define oz 1.5\n#define hex_size vec2(0.5 - p.y * 0.1, 10)\nvoid common_map(vec3 p, out float df0, out float df1)\n{\ndf0 = p.y - 1.0 + sin(length(p.xz) * 0.8 - uTime);\nvec3 q0 = p;\nq0.x = mod_conv(q0.x - ox, ox + ox) - ox;\nq0.z = mod_conv(q0.z - oz * 0.5, oz) - oz * 0.5;\nfloat hex0 = sdHexPrism(q0.xzy, hex_size) - 0.2;\nvec3 q1_c = p;\nq1_c.x = mod_conv(q1_c.x, ox + ox) - ox;\nq1_c.z = mod_conv(q1_c.z, oz) - oz * 0.5;\nfloat hex1 = sdHexPrism(q1_c.xzy, hex_size) - 0.2;\ndf1 = min(hex0, hex1);\n}\nfloat smin( float a, float b, float k )\n{\nfloat h = clamp( 0.5 + 0.5*(b-a)/k, 0.0, 1.0 );\nreturn mix( b, a, h ) - k*h*(1.0-h);\n}\nfloat smax(float a, float b, float k)\n{\nreturn smin(a, b, -k);\n}\nfloat map(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nreturn smax(df0, df1, 0.1);\n}\nfloat mat(vec3 p)\n{\nfloat df0, df1;\ncommon_map(p, df0, df1);\nfloat res = 0;\nif (df0 > df1) {\nres = 1;\n}\nelse {\nres = 0;\n}\nreturn res;\n}\nvec3 getNormal(vec3 p, float prec)\n{\nvec3 e = vec3(prec, 0.0 , 0.0 );\nreturn normalize(vec3(\nmap(p+e)-map(p-e),\nmap(p+e.yxz)-map(p-e.yxz),\nmap(p+e.zyx)-map(p-e.zyx)));\n}\nfloat getAmbiantOcclusion(vec3 p, vec3 n, float k)\n{\nconst float aoStep = 0.1;\nfloat occl = 0.;\nfor(int i = min(uFrame,0); i < 6; ++i)\n{\nfloat diff = float(i)*aoStep;\nfloat d = map(p + n*diff);\noccl += (diff - d) * pow(2., float(-i));\n}\nreturn min(1., 1. - k*occl);\n}\nfloat getShadow(vec3 ro, vec3 rd, float minD, float maxD, float k)\n{\nfloat res = 1.0;\nfloat d = minD;\nfloat s = 0.;\nfor(int i = min(uFrame,0); i < 20; ++i)\n{\ns = map(ro + rd * d);\nif( abs(s)<d*d * 1.0e - 5.0 ) {\nres = 0;\n// return 0.0;\n}\nelse {\nres = min( res, k * s / d );\nd += s;\n// CONV: break unsupported, see Milkwave manual\nif(d >= maxD) i=20;\n}\n}\nreturn res;\n}\nvec3 cam(vec2 uv_conv,  vec3 ro, vec3 cv, float fov)\n{\nvec3 cu = normalize(vec3( 0.0 , 1.0 , 0.0 ));\nvec3 z = normalize(cv-ro);\nvec3 x = normalize(cross(cu,z));\nvec3 y = cross(z,x);\nreturn normalize(z + fov*uv_conv.x*x + fov*uv_conv.y*y);\n}\nvec3 hsv2rgb_smooth( in vec3 c )\n{\nvec3 rgb = clamp( abs(mod_conv(c.x*6.0+vec3(0.0,4.0,2.0),6.0)-3.0)-1.0, 0.0, 1.0 );\nrgb = rgb*rgb*(3.0-2.0*rgb);\n// cubic smoothing\n#ifdef bass_smooth\nfloat fac = (int)vis_version > 2 ? 1.0 * vis_intensity+vis_shift : (0.65 + smoothstep( 0.0 , 2.0 ,(int)vis_version == 1 ? bass_smooth : audioLowSmooth)*0.5)*vis_intensity+vis_shift;\n#else\nfloat fac = 0.8 + clamp(audioLowSmooth,0.6,1.2)*0.2;\n#endif\nreturn c.z * fac * mix( vec3(1.0, 1.0, 1.0), rgb, c.y);\n}\nelse {\ns = map(ro + rd * d);\nd += s * 0.5;\n}\n}\nif (d < md)\n{\n// surface point\nvec3 p = ro + rd * d;\n// surface normal, precision of 0.1 for remove some aliasing\nvec3 n = getNormal(p, 0.1);\n// light pos\nvec3 lp = vec3( 0.0 , 5.0 , 0.0 );\n// light dir\nvec3 ld = normalize(lp - p);\n// diffuse, ambiant occlusion, shadow, specular\nfloat diff = pow(dot(n, ld) * .5 + .5,2.0);\nfloat ao = getAmbiantOcclusion(p, n, 40.0);\nfloat sha = clamp(getShadow(p, ld, 0.01, 150.0, 5.0), 0. ,0.9);\nfloat spe = pow(max(dot(-rd, reflect(-ld, n)), 0.0), 32.0);\nif (mat(p) < 0.5)\n// hexa sides\n{\n// smooth hsv\nvec3 base = hsv2rgb_smooth(vec3(atan(p.x,p.z)/3.14159*0.5 - uTime * 0.1, 0.8, 0.8));\n// vary base color according to ao\ncol = mix(base, vec3( 1.0 , 1.0 , 1.0), ao) * 0.5;\n}\nelse\n// hexa face\n{\n// reflected gold\ncol = vec3(1.0, 0.85, 0.0) * texture(iChannel0, reflect(rd, n)).rgb;\n}\n// final brdf\ncol += diff * sha * 0.5 + spe;\n// clamp for avoid overlight\ncol = clamp(col, 0., 1.);\n}\n// distance fog\ncol *= exp(1.0-d*d*0.001);\n// final color\nfragColor = vec4(col, 1.0 );\nret = fragColor;\n}\nshader_body {\nvec4 fragColor = 0;\n// CONV: Center on screen, then try some uAspect correction\nuv = (uv * 2.0) - 1.0;\nuv *= uAspect.xy;\n// CONV: Adjust this to flip the output (±uv.x, ±uv.y)\nuv = vec2(uv.x, -uv.y);\nvec2 fragCoord  = uv;\nvec2 si = uv.xy;\n// central uv\nvec2 uvc = uv;\n// (2.*fragCoord.xy-si)/si.y;\n// classice turning table camera\nfloat a = uTime * 0.1 + THUMBNAIL_ANGLE_OFFSET;\nvec3 ro = vec3(cos(a), 0.0, sin(a)) * 20.0;\nro.y = 20.0;\nvec3 rd = cam(uvc, ro, vec3( 0.0 , 0.0 , 0.0 ), 0.4);\nvec3 col = vec3(0.1, 0.1, 0.1);\n// log raymarching\nfloat s = 1., d = 0., md = 100.;\nfor (int i = min(uFrame,0); i < LOOPS; i++)\n{\n// CONV: break unsupported, see Milkwave manual\nif (d*d/s>1e8 || d > 70.) {\ni=LOOPS;\n}"
          },
          "warnings": [],
          "errors": [],
          "diagnostics": {
            "lineCount": 313,
            "sourceLength": 9689,
            "issueCount": 2,
            "issues": [
              {
                "severity": "warning",
                "code": "noise-volume-sampler",
                "message": "References Milkwave volume-noise samplers that are not currently bound by the runtime."
              },
              {
                "severity": "warning",
                "code": "custom-sampler",
                "message": "References custom sampler names; imported texture-slot assets are not currently provisioned in the runtime."
              }
            ]
          }
        }
      }
    }
  }
}