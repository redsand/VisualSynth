{
  "version": 6,
  "metadata": {
    "version": 6,
    "name": "COmplexEgison",
    "author": "Unknown",
    "source": "COmplexEgison.milk",
    "importedFrom": "Milkwave",
    "createdAt": "2026-03-20T00:49:33.174Z",
    "updatedAt": "2026-03-20T00:49:33.174Z",
    "category": "Audio Reactive",
    "compatibility": {
      "minVersion": "1.4.0"
    },
    "milkwave": {
      "format": "milkwave-ir",
      "version": 1,
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-warp",
        "custom-comp",
        "preset-init",
        "preset-frame",
        "preset-pixel",
        "blur-samplers",
        "volume-noise"
      ],
      "reasons": [],
      "translation": {
        "pipeline": "milkwave-offline-v1",
        "runtimePatchRecommended": true,
        "generatedPasses": [
          "warp",
          "comp"
        ]
      }
    },
    "activeEngineId": "engine-radial-core",
    "activeModeId": "mode-cosmic",
    "intendedMusicStyle": "Electronic",
    "visualIntentTags": [
      "imported",
      "milkwave"
    ],
    "colorChemistry": [
      "analog",
      "balanced"
    ],
    "defaultTransition": {
      "durationMs": 600,
      "curve": "easeInOut"
    }
  },
  "scenes": [
    {
      "id": "scene-1",
      "scene_id": "scene-1",
      "name": "Main",
      "intent": "ambient",
      "duration": 0,
      "transition_in": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "transition_out": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "trigger": {
        "type": "manual"
      },
      "assigned_layers": {
        "core": [],
        "support": [
          "layer-milkwave"
        ],
        "atmosphere": []
      },
      "layers": [
        {
          "id": "layer-milkwave",
          "name": "Milkwave",
          "role": "support",
          "enabled": true,
          "opacity": 2.3,
          "blendMode": "screen",
          "transform": {
            "x": 0,
            "y": 0,
            "scale": 1,
            "rotation": 0
          },
          "params": {
            "opacity": 2.3,
            "enabled": true,
            "blendMode": "screen",
            "transform": {
              "x": 0,
              "y": 0,
              "scale": 1,
              "rotation": 0
            },
            "zoom": 1,
            "rotation": 0,
            "warp": 0.01,
            "decay": 0.965,
            "gamma": 2.3,
            "bassSensitivity": 1,
            "midSensitivity": 1,
            "trebSensitivity": 1
          }
        }
      ]
    }
  ],
  "activeSceneId": "scene-1",
  "roleWeights": {
    "core": 1,
    "support": 1,
    "atmosphere": 1
  },
  "tempoSync": {
    "bpm": 120,
    "source": "manual"
  },
  "modulations": [],
  "macros": [],
  "_shaderData": {
    "warp": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nvec2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan(uv_center.y, uv_center.x);\n// Sample current uFrame\nvec3 current = texture(sampler_main, uv).xyz;\nret = current;\nvec3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length((vec2(1.0)/uTexSize)) * 6.0;\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nvec3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nvec4 consciousness_noise = tex3D(sampler_noisevol_hq,\nvec3(uv * uTexSize.xy * texsize_noisevol_hq.zw * noise_scale,\nuTime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2.0 + dist * 5.0) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8.0 + uTime * 2.0) * sin(dist * 15.0 - uTime * 3.0);\nif (rift_pattern > 0.5) {\nret = vec3(1.0) - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = clamp01(ret);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nvec2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan(uv_center.y, uv_center.x);\n// Sample current uFrame\nvec3 current = texture(sampler_main, uv).xyz;\nret = current;\nvec3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length((vec2(1.0)/uTexSize)) * 6.0;\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nvec3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nvec4 consciousness_noise = tex3D(sampler_noisevol_hq,\nvec3(uv * uTexSize.xy * texsize_noisevol_hq.zw * noise_scale,\nuTime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2.0 + dist * 5.0) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8.0 + uTime * 2.0) * sin(dist * 15.0 - uTime * 3.0);\nif (rift_pattern > 0.5) {\nret = vec3(1.0) - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = clamp01(ret);\n}\n  fragColor = vec4(ret, 1.0);\n}",
    "comp": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nvec2 fine_d = (vec2(1.0)/uTexSize) * 4.0;\nvec2 med_d = (vec2(1.0)/uTexSize) * 8.0;\nvec2 coarse_d = (vec2(1.0)/uTexSize) * 16.0;\nvec3 fine_dx = GetPixel(uv + vec2( 1.0 , 0.0 ) * fine_d) - GetPixel(uv - vec2( 1.0 , 0.0 ) * fine_d).xy;\nvec3 fine_dy = GetPixel(uv + vec2( 0.0 , 1.0 ) * fine_d) - GetPixel(uv - vec2( 0.0 , 1.0 ) * fine_d).xy;\nvec3 med_dx = GetBlur1(uv + vec2( 1.0 , 0.0 ) * med_d) - GetBlur1(uv - vec2( 1.0 , 0.0 ) * med_d).xy;\nvec3 med_dy = GetBlur1(uv + vec2( 0.0 , 1.0 ) * med_d) - GetBlur1(uv - vec2( 0.0 , 1.0 ) * med_d).xy;\nvec3 coarse_dx = GetBlur2(uv + vec2( 1.0 , 0.0 ) * coarse_d) - GetBlur2(uv - vec2( 1.0 , 0.0 ) * coarse_d).xy;\nvec3 coarse_dy = GetBlur2(uv + vec2( 0.0 , 1.0 ) * coarse_d) - GetBlur2(uv - vec2( 0.0 , 1.0 ) * coarse_d).xy;\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nvec4 synaptic_noise = tex3D(sampler_noisevol_hq,\nvec3((uv + vec2(lum(fine_dx), lum(fine_dy)) * 0.5) *\nuTexSize.xy * texsize_noisevol_hq.zw * tex_scale,\nuTime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nvec2 field_force = vec2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += vec2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nvec3 layer_fine = GetPixel(uv);\nvec3 layer_med = GetBlur1(uv);\nvec3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, vec3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = mix(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = clamp01(ret);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nvec2 fine_d = (vec2(1.0)/uTexSize) * 4.0;\nvec2 med_d = (vec2(1.0)/uTexSize) * 8.0;\nvec2 coarse_d = (vec2(1.0)/uTexSize) * 16.0;\nvec3 fine_dx = GetPixel(uv + vec2( 1.0 , 0.0 ) * fine_d) - GetPixel(uv - vec2( 1.0 , 0.0 ) * fine_d).xy;\nvec3 fine_dy = GetPixel(uv + vec2( 0.0 , 1.0 ) * fine_d) - GetPixel(uv - vec2( 0.0 , 1.0 ) * fine_d).xy;\nvec3 med_dx = GetBlur1(uv + vec2( 1.0 , 0.0 ) * med_d) - GetBlur1(uv - vec2( 1.0 , 0.0 ) * med_d).xy;\nvec3 med_dy = GetBlur1(uv + vec2( 0.0 , 1.0 ) * med_d) - GetBlur1(uv - vec2( 0.0 , 1.0 ) * med_d).xy;\nvec3 coarse_dx = GetBlur2(uv + vec2( 1.0 , 0.0 ) * coarse_d) - GetBlur2(uv - vec2( 1.0 , 0.0 ) * coarse_d).xy;\nvec3 coarse_dy = GetBlur2(uv + vec2( 0.0 , 1.0 ) * coarse_d) - GetBlur2(uv - vec2( 0.0 , 1.0 ) * coarse_d).xy;\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nvec4 synaptic_noise = tex3D(sampler_noisevol_hq,\nvec3((uv + vec2(lum(fine_dx), lum(fine_dy)) * 0.5) *\nuTexSize.xy * texsize_noisevol_hq.zw * tex_scale,\nuTime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nvec2 field_force = vec2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += vec2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nvec3 layer_fine = GetPixel(uv);\nvec3 layer_med = GetBlur1(uv);\nvec3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, vec3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = mix(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = clamp01(ret);\n}\n  fragColor = vec4(ret, 1.0);\n}",
    "perFrameCode": [
      "// === AUDITORY CORTEX PROCESSING ===",
      "// Multi-band energy envelope followers",
      "energy_total = (bass + mid + treb) * 0.333;",
      "bass_inertia = bass_inertia * 0.85 + bass * 0.15;",
      "mid_resonance = mid_resonance * 0.88 + mid * 0.12;",
      "treb_harmonic = treb_harmonic * 0.90 + treb * 0.10;",
      "",
      "// Beat detection with temporal memory",
      "beat_threshold = 1.15 + beat_memory * 0.3;",
      "is_beat = above(bass, beat_threshold) * (1 - equal(beat_memory, 1));",
      "beat_memory = if(is_beat, 1, beat_memory * 0.92);",
      "",
      "// Neural accumulation creates flux energy",
      "flux_energy = flux_energy * 0.96 + (bass_inertia * 0.4 + mid_resonance * 0.35 + treb_harmonic * 0.25);",
      "q8 = min(flux_energy * 1.5, 3); // Global intensity",
      "",
      "// === TEMPORAL PHASE ENGINE ===",
      "neural_speed = 0.018 + flux_energy * 0.025 + is_beat * 0.12;",
      "neural_phase = neural_phase + neural_speed * (fps / 60);",
      "q1 = neural_phase;",
      "",
      "// Breathing waveform generator",
      "breath_rate = 0.4 + bass_inertia * 0.2;",
      "breath = 0.5 + 0.5 * sin(time * breath_rate + neural_phase * 0.5);",
      "q2 = breath;",
      "",
      "// Dimensional ripple spacetime distortion",
      "dimensional_ripple = dimensional_ripple * 0.88 + is_beat * 0.6;",
      "q3 = dimensional_ripple;",
      "",
      "// === TRANSFORMATION MATRIX ===",
      "// Zoom breathes with neural energy",
      "zoom_breath = sin(neural_phase * 0.3 + time * 0.2) * 0.008;",
      "zoom_pulse = 1 - bass_inertia * 0.015 + is_beat * 0.03;",
      "zoom = zoom_pulse + zoom_breath;",
      "",
      "// Rotational inertia from frequency interaction",
      "rot_inertia = (bass_inertia - mid_resonance) * 0.01 + sin(time * 0.15) * 0.002;",
      "rot = rot + rot_inertia + is_beat * 0.05;",
      "",
      "// Spatial drift through neural space",
      "cx = 0.5 + sin(neural_phase * 0.2 + time * 0.1) * 0.06;",
      "cx = cx + cos(bass_inertia * 3) * 0.03;",
      "cy = 0.5 + cos(neural_phase * 0.15 + time * 0.12) * 0.06;",
      "cy = cy + sin(mid_resonance * 3) * 0.03;",
      "",
      "// Directional flow vectors",
      "dx = sin(neural_phase * 0.4) * 0.001 + cos(time * 0.25) * 0.0005;",
      "dy = cos(neural_phase * 0.35) * 0.001 + sin(time * 0.3) * 0.0005;",
      "",
      "// Spacetime warping intensity",
      "warp_field = 0.01 + breath * 0.05 * flux_energy;",
      "warp_field = warp_field + dimensional_ripple * 0.08;",
      "warp = min(warp_field, 0.4);",
      "",
      "// Aspect ratio modulation",
      "sx = 1 + sin(neural_phase * 0.6 + time * 0.2) * 0.02;",
      "sy = 1 + cos(neural_phase * 0.55 + time * 0.18) * 0.02;",
      "",
      "// === CHROMA NEURAL MAPPING ===",
      "// RGB channels driven by separate frequency bands",
      "wave_r = 0.5 + 0.5 * sin(neural_phase + bass_inertia * 4);",
      "wave_g = 0.5 + 0.5 * sin(neural_phase * 1.3 + mid_resonance * 4 + 2.1);",
      "wave_b = 0.5 + 0.5 * sin(neural_phase * 1.7 + treb_harmonic * 4 + 4.2);",
      "",
      "// Wave position follows neural gravity",
      "wave_x = 0.5 + sin(neural_phase + time * 0.3) * 0.25;",
      "wave_y = 0.5 + cos(neural_phase * 0.8 + time * 0.25) * 0.25;",
      "",
      "// Adaptive decay creates temporal smearing",
      "decay = 0.92 - flux_energy * 0.02 - is_beat * 0.04;",
      "decay = max(0.88, min(0.98, decay));",
      "",
      "// Motion vectors visualize energy flow",
      "mv_a = 0.2 + flux_energy * 0.3;",
      "mv_r = 0.5 + sin(neural_phase * 0.3) * 0.5;",
      "mv_g = 0.5 + sin(neural_phase * 0.4 + 2.1) * 0.5;",
      "mv_b = 0.5 + sin(neural_phase * 0.5 + 4.2) * 0.5;"
    ],
    "perFrameInitCode": [
      "// === NEURAL FLUX INITIALIZATION ===",
      "neural_phase = 0;",
      "flux_energy = 0;",
      "bass_inertia = 0;",
      "mid_resonance = 0;",
      "treb_harmonic = 0;",
      "beat_memory = 0;",
      "dimensional_ripple = 0;",
      "memory_decay = 0.95;"
    ],
    "perPixelCode": [
      "// === NEURAL FIELD DISTORTION CALCULUS ===",
      "// Establish relative coordinates from center",
      "rel_x = x - cx;",
      "rel_y = y - cy;",
      "dist = sqrt(rel_x * rel_x + rel_y * rel_y);",
      "ang = atan2(rel_y, rel_x);",
      "",
      "// Primary neural interference pattern",
      "neural_wave1 = sin(dist * 16 - neural_phase * 5 + ang * 4) * bass_inertia;",
      "neural_wave2 = sin(dist * 12 + neural_phase * 4 - ang * 6) * mid_resonance;",
      "neural_wave3 = sin(dist * 20 - neural_phase * 3 + ang * 8) * treb_harmonic;",
      "",
      "// Frequency-specific field generators",
      "bass_field = cos(ang * 5 + dist * 8 + neural_phase * 2) * (bass_inertia * 0.5);",
      "mid_field = sin(ang * 7 - dist * 12 + neural_phase * 3) * (mid_resonance * 0.5);",
      "treb_field = cos(ang * 9 + dist * 16 - neural_phase * 4) * (treb_harmonic * 0.5);",
      "",
      "// Quantum superposition of all fields",
      "total_field = neural_wave1 * 0.35 + neural_wave2 * 0.3 + neural_wave3 * 0.2;",
      "total_field = total_field + bass_field * 0.4 + mid_field * 0.35 + treb_field * 0.3;",
      "",
      "// Dimensional pulse propagation",
      "dimensional_pulse = sin(dist * 30 - time * 3 + flux_energy * 6);",
      "",
      "// Angular quantum segmentation",
      "angular_quantum = sin(ang * 15 + time * 1.5 + dimensional_ripple * 4);",
      "",
      "// === SPACETIME DISPLACEMENT ===",
      "displacement_magnitude = (total_field + dimensional_pulse * 0.5 + angular_quantum * 0.25) * 0.02;",
      "displacement_magnitude = displacement_magnitude * (1 + breath * 0.5);",
      "",
      "// Apply to coordinate system",
      "zoom = zoom + displacement_magnitude * sin(dist * 12);",
      "rot = rot + displacement_magnitude * 0.5 * cos(ang * 4);",
      "",
      "// Dimensional warping creates reality folds",
      "dim_factor = dimensional_ripple * sin(dist * 20 + ang * 6);",
      "zoom = zoom + dim_factor * 0.04;"
    ],
    "waves": [],
    "shapes": [],
    "originalParameters": {
      "fRating": 4.5,
      "fGammaAdj": 2.3,
      "fDecay": 0.965,
      "fVideoEchoZoom": 1.025,
      "fVideoEchoAlpha": 0.45,
      "nVideoEchoOrientation": false,
      "nWaveMode": false,
      "bAdditiveWaves": true,
      "bWaveDots": false,
      "bWaveThick": true,
      "bModWaveAlphaByVolume": false,
      "bMaximizeWaveColor": true,
      "bTexWrap": true,
      "bDarkenCenter": false,
      "bRedBlueStereo": false,
      "bBrighten": false,
      "bDarken": true,
      "bSolarize": false,
      "bInvert": false,
      "fWaveAlpha": 0.2,
      "fWaveScale": 1.45,
      "fWaveSmoothing": 0.1,
      "fWaveParam": 0.2,
      "fModWaveAlphaStart": 0.65,
      "fModWaveAlphaEnd": 1.5,
      "fWarpAnimSpeed": 1,
      "fWarpScale": 1.331,
      "fZoomExponent": 1,
      "fShader": 0.5,
      "zoom": 1,
      "rot": 0,
      "cx": 0.5,
      "cy": 0.5,
      "dx": 0,
      "dy": 0,
      "warp": 0.01,
      "sx": 1,
      "sy": 1,
      "wave_r": 0.5,
      "wave_g": 0.5,
      "wave_b": 0.5,
      "wave_x": 0,
      "wave_y": 0.5,
      "ob_size": 0,
      "ob_r": 0,
      "ob_g": 0,
      "ob_b": 0,
      "ob_a": 0,
      "ib_size": 0.01,
      "ib_r": 0.25,
      "ib_g": 0.25,
      "ib_b": 0.25,
      "ib_a": 0,
      "nMotionVectorsX": false,
      "nMotionVectorsY": false,
      "mv_dx": 0,
      "mv_dy": 0,
      "mv_l": 5,
      "mv_r": 1,
      "mv_g": 1,
      "mv_b": 0,
      "mv_a": 0,
      "b1n": false,
      "b2n": false,
      "b3n": false,
      "b1x": false,
      "b2x": false,
      "b3x": false,
      "b1ed": false
    },
    "translation": {
      "pipeline": "milkwave-offline-v1",
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-warp",
        "custom-comp",
        "preset-init",
        "preset-frame",
        "preset-pixel",
        "blur-samplers",
        "volume-noise"
      ],
      "runtimePatchRecommended": true,
      "passes": {
        "warp": {
          "kind": "warp",
          "requested": true,
          "generated": true,
          "source": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nvec2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan(uv_center.y, uv_center.x);\n// Sample current uFrame\nvec3 current = texture(sampler_main, uv).xyz;\nret = current;\nvec3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length((vec2(1.0)/uTexSize)) * 6.0;\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nvec3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nvec4 consciousness_noise = tex3D(sampler_noisevol_hq,\nvec3(uv * uTexSize.xy * texsize_noisevol_hq.zw * noise_scale,\nuTime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2.0 + dist * 5.0) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8.0 + uTime * 2.0) * sin(dist * 15.0 - uTime * 3.0);\nif (rift_pattern > 0.5) {\nret = vec3(1.0) - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = clamp01(ret);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nvec2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan(uv_center.y, uv_center.x);\n// Sample current uFrame\nvec3 current = texture(sampler_main, uv).xyz;\nret = current;\nvec3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length((vec2(1.0)/uTexSize)) * 6.0;\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nvec3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nvec4 consciousness_noise = tex3D(sampler_noisevol_hq,\nvec3(uv * uTexSize.xy * texsize_noisevol_hq.zw * noise_scale,\nuTime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2.0 + dist * 5.0) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8.0 + uTime * 2.0) * sin(dist * 15.0 - uTime * 3.0);\nif (rift_pattern > 0.5) {\nret = vec3(1.0) - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = clamp01(ret);\n}\n  fragColor = vec4(ret, 1.0);\n}",
          "backend": "milkwave-direct-v2",
          "normalized": {
            "preludeLineCount": 69,
            "bodyLineCount": 0,
            "helperLineCount": 7,
            "hasShaderBodyBlock": false,
            "source": "shader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nfloat2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan2(uv_center.y, uv_center.x);\n// Sample current frame\nfloat3 current = tex2D(sampler_main, uv).xyz;\nret = current;\nfloat3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length(texsize.zw) * 6;\nret = max(ret, tex2D(sampler_main, (uv - 0.5) * (1 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, tex2D(sampler_main, (uv - 0.5) * (1 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nfloat3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nfloat4 consciousness_noise = tex3D(sampler_noisevol_hq,\nfloat3(uv * texsize.xy * texsize_noisevol_hq.zw * noise_scale,\ntime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2 + dist * 5) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8 + time * 2) * sin(dist * 15 - time * 3);\nif (rift_pattern > 0.5) {\nret = 1.0 - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = saturate(ret);\n}",
            "dialectSource": "shader_body\n{\n// === QUANTUM CONSCIOUSNESS WARP SHADER ===\n// Multi-dimensional reality warping with musical intelligence\nvec2 uv_center = uv - 0.5;\nfloat dist = length(uv_center);\nfloat angle = atan(uv_center.y, uv_center.x);\n// Sample current uFrame\nvec3 current = texture(sampler_main, uv).xyz;\nret = current;\nvec3 original = ret;\n// === ORGANIC CONSCIOUSNESS SPREADING ===\n// Neural growth pattern\nfloat spread_amount = length((vec2(1.0)/uTexSize)) * 6.0;\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 - spread_amount) + 0.5).xyz * 0.95);\nret = max(ret, texture(sampler_main, (uv - 0.5) * (1.0 + spread_amount) + 0.5).xyz * 0.95);\n// === DIMENSIONAL FEEDBACK LOOPS ===\n// Create depth through blur differential\nvec3 blur_diff = ret - GetBlur1(uv);\nret += blur_diff * 0.25;\n// === VOLUMETRIC CONSCIOUSNESS NOISE ===\n// 3D noise injection for texture complexity\nfloat noise_scale = 0.08;\nfloat noise_speed = 0.15;\nvec4 consciousness_noise = tex3D(sampler_noisevol_hq,\nvec3(uv * uTexSize.xy * texsize_noisevol_hq.zw * noise_scale,\nuTime * noise_speed + q1 * 0.3));\n// === QUANTUM COLOR SHIFTING ===\n// Musical intelligence drives channel manipulation\nfloat energy_threshold = q8;\nfloat consciousness_phase = q1;\nfloat dimensional_shift = q3;\n// Phase-based color rotation\nif (energy_threshold > 0.5) {\nfloat shift_factor = sin(consciousness_phase * 2.0 + dist * 5.0) * 0.5 + 0.5;\nif (shift_factor > 0.6) {\nret.x = original.z * (1.0 - consciousness_noise.x * 0.3);\nret.y = original.x * (1.0 - consciousness_noise.y * 0.3);\nret.z = original.y * (1.0 - consciousness_noise.z * 0.3);\n}\n}\n// === CONDITIONAL NOISE INJECTION ===\n// Creates musical-reactive texture patterns\nfloat noise_threshold = 0.3 + energy_threshold * 0.2;\nif (ret.x > noise_threshold && ret.x < 0.8) {\nret.y += consciousness_noise.x * 0.4 * energy_threshold;\n}\nif (ret.y > noise_threshold && ret.y < 0.8) {\nret.z += consciousness_noise.y * 0.4 * energy_threshold;\n}\nif (ret.z > noise_threshold && ret.z < 0.8) {\nret.x += consciousness_noise.z * 0.4 * energy_threshold;\n}\n// === DIMENSIONAL RIFT EFFECT ===\n// Strong dimensional shifts create reality tears\nif (dimensional_shift > 0.3) {\nfloat rift_pattern = sin(angle * 8.0 + uTime * 2.0) * sin(dist * 15.0 - uTime * 3.0);\nif (rift_pattern > 0.5) {\nret = vec3(1.0) - ret * 0.8;\n}\n}\n// === CONSCIOUSNESS DECAY ===\n// Intelligent temporal fade preserves intense moments\nfloat decay_rate = 0.018 - energy_threshold * 0.012;\ndecay_rate = decay_rate - dimensional_shift * 0.01;\nret -= max(0.005, decay_rate);\n// Ensure valid color range\nret = clamp01(ret);\n}"
          },
          "warnings": [],
          "errors": [],
          "diagnostics": {
            "lineCount": 251,
            "sourceLength": 9611,
            "issueCount": 1,
            "issues": [
              {
                "severity": "warning",
                "code": "noise-volume-sampler",
                "message": "References Milkwave volume-noise samplers that are not currently bound by the runtime."
              }
            ]
          }
        },
        "comp": {
          "kind": "comp",
          "requested": true,
          "generated": true,
          "source": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nvec2 fine_d = (vec2(1.0)/uTexSize) * 4.0;\nvec2 med_d = (vec2(1.0)/uTexSize) * 8.0;\nvec2 coarse_d = (vec2(1.0)/uTexSize) * 16.0;\nvec3 fine_dx = GetPixel(uv + vec2( 1.0 , 0.0 ) * fine_d) - GetPixel(uv - vec2( 1.0 , 0.0 ) * fine_d).xy;\nvec3 fine_dy = GetPixel(uv + vec2( 0.0 , 1.0 ) * fine_d) - GetPixel(uv - vec2( 0.0 , 1.0 ) * fine_d).xy;\nvec3 med_dx = GetBlur1(uv + vec2( 1.0 , 0.0 ) * med_d) - GetBlur1(uv - vec2( 1.0 , 0.0 ) * med_d).xy;\nvec3 med_dy = GetBlur1(uv + vec2( 0.0 , 1.0 ) * med_d) - GetBlur1(uv - vec2( 0.0 , 1.0 ) * med_d).xy;\nvec3 coarse_dx = GetBlur2(uv + vec2( 1.0 , 0.0 ) * coarse_d) - GetBlur2(uv - vec2( 1.0 , 0.0 ) * coarse_d).xy;\nvec3 coarse_dy = GetBlur2(uv + vec2( 0.0 , 1.0 ) * coarse_d) - GetBlur2(uv - vec2( 0.0 , 1.0 ) * coarse_d).xy;\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nvec4 synaptic_noise = tex3D(sampler_noisevol_hq,\nvec3((uv + vec2(lum(fine_dx), lum(fine_dy)) * 0.5) *\nuTexSize.xy * texsize_noisevol_hq.zw * tex_scale,\nuTime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nvec2 field_force = vec2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += vec2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nvec3 layer_fine = GetPixel(uv);\nvec3 layer_med = GetBlur1(uv);\nvec3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, vec3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = mix(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = clamp01(ret);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nvec2 fine_d = (vec2(1.0)/uTexSize) * 4.0;\nvec2 med_d = (vec2(1.0)/uTexSize) * 8.0;\nvec2 coarse_d = (vec2(1.0)/uTexSize) * 16.0;\nvec3 fine_dx = GetPixel(uv + vec2( 1.0 , 0.0 ) * fine_d) - GetPixel(uv - vec2( 1.0 , 0.0 ) * fine_d).xy;\nvec3 fine_dy = GetPixel(uv + vec2( 0.0 , 1.0 ) * fine_d) - GetPixel(uv - vec2( 0.0 , 1.0 ) * fine_d).xy;\nvec3 med_dx = GetBlur1(uv + vec2( 1.0 , 0.0 ) * med_d) - GetBlur1(uv - vec2( 1.0 , 0.0 ) * med_d).xy;\nvec3 med_dy = GetBlur1(uv + vec2( 0.0 , 1.0 ) * med_d) - GetBlur1(uv - vec2( 0.0 , 1.0 ) * med_d).xy;\nvec3 coarse_dx = GetBlur2(uv + vec2( 1.0 , 0.0 ) * coarse_d) - GetBlur2(uv - vec2( 1.0 , 0.0 ) * coarse_d).xy;\nvec3 coarse_dy = GetBlur2(uv + vec2( 0.0 , 1.0 ) * coarse_d) - GetBlur2(uv - vec2( 0.0 , 1.0 ) * coarse_d).xy;\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nvec4 synaptic_noise = tex3D(sampler_noisevol_hq,\nvec3((uv + vec2(lum(fine_dx), lum(fine_dy)) * 0.5) *\nuTexSize.xy * texsize_noisevol_hq.zw * tex_scale,\nuTime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nvec2 field_force = vec2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += vec2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nvec3 layer_fine = GetPixel(uv);\nvec3 layer_med = GetBlur1(uv);\nvec3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, vec3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = mix(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = clamp01(ret);\n}\n  fragColor = vec4(ret, 1.0);\n}",
          "backend": "milkwave-direct-v2",
          "normalized": {
            "preludeLineCount": 50,
            "bodyLineCount": 0,
            "helperLineCount": 0,
            "hasShaderBodyBlock": false,
            "source": "shader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nfloat2 fine_d = texsize.zw * 4;\nfloat2 med_d = texsize.zw * 8;\nfloat2 coarse_d = texsize.zw * 16;\nfloat3 fine_dx = GetPixel(uv + float2(1,0) * fine_d) - GetPixel(uv - float2(1,0) * fine_d);\nfloat3 fine_dy = GetPixel(uv + float2(0,1) * fine_d) - GetPixel(uv - float2(0,1) * fine_d);\nfloat3 med_dx = GetBlur1(uv + float2(1,0) * med_d) - GetBlur1(uv - float2(1,0) * med_d);\nfloat3 med_dy = GetBlur1(uv + float2(0,1) * med_d) - GetBlur1(uv - float2(0,1) * med_d);\nfloat3 coarse_dx = GetBlur2(uv + float2(1,0) * coarse_d) - GetBlur2(uv - float2(1,0) * coarse_d);\nfloat3 coarse_dy = GetBlur2(uv + float2(0,1) * coarse_d) - GetBlur2(uv - float2(0,1) * coarse_d);\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nfloat4 synaptic_noise = tex3D(sampler_noisevol_hq,\nfloat3((uv + float2(lum(fine_dx), lum(fine_dy)) * 0.5) *\ntexsize.xy * texsize_noisevol_hq.zw * tex_scale,\ntime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nfloat2 field_force = float2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += float2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nfloat3 layer_fine = GetPixel(uv);\nfloat3 layer_med = GetBlur1(uv);\nfloat3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, float3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = lerp(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = saturate(ret);\n}",
            "dialectSource": "shader_body\n{\nfloat neural_power = q8;\nfloat phase = q1;\nfloat chaos = q2;\nuv = 0.5 + (uv - 0.5) * 0.98;\n// Multi-resolution gradient analysis\nvec2 fine_d = (vec2(1.0)/uTexSize) * 4.0;\nvec2 med_d = (vec2(1.0)/uTexSize) * 8.0;\nvec2 coarse_d = (vec2(1.0)/uTexSize) * 16.0;\nvec3 fine_dx = GetPixel(uv + vec2( 1.0 , 0.0 ) * fine_d) - GetPixel(uv - vec2( 1.0 , 0.0 ) * fine_d).xy;\nvec3 fine_dy = GetPixel(uv + vec2( 0.0 , 1.0 ) * fine_d) - GetPixel(uv - vec2( 0.0 , 1.0 ) * fine_d).xy;\nvec3 med_dx = GetBlur1(uv + vec2( 1.0 , 0.0 ) * med_d) - GetBlur1(uv - vec2( 1.0 , 0.0 ) * med_d).xy;\nvec3 med_dy = GetBlur1(uv + vec2( 0.0 , 1.0 ) * med_d) - GetBlur1(uv - vec2( 0.0 , 1.0 ) * med_d).xy;\nvec3 coarse_dx = GetBlur2(uv + vec2( 1.0 , 0.0 ) * coarse_d) - GetBlur2(uv - vec2( 1.0 , 0.0 ) * coarse_d).xy;\nvec3 coarse_dy = GetBlur2(uv + vec2( 0.0 , 1.0 ) * coarse_d) - GetBlur2(uv - vec2( 0.0 , 1.0 ) * coarse_d).xy;\n// Gradient strength\nfloat gradient_strength = length(fine_dx) + length(med_dx) + length(coarse_dx);\ngradient_strength += length(fine_dy) + length(med_dy) + length(coarse_dy);\n// 3D noise with gradient coupling\nfloat tex_scale = 0.08 + chaos * 0.03;\nfloat time_scale = 0.25 + phase * 0.1;\nvec4 synaptic_noise = tex3D(sampler_noisevol_hq,\nvec3((uv + vec2(lum(fine_dx), lum(fine_dy)) * 0.5) *\nuTexSize.xy * texsize_noisevol_hq.zw * tex_scale,\nuTime * time_scale + phase * 0.3));\n// Gradient-driven displacement\nvec2 field_force = vec2(lum(med_dx), lum(med_dy)) * 0.08;\nfield_force += vec2(lum(coarse_dx), lum(coarse_dy)) * 0.05;\nuv -= field_force * (0.6 + neural_power * 0.4);\n// Multi-layer composite\nvec3 layer_fine = GetPixel(uv);\nvec3 layer_med = GetBlur1(uv);\nvec3 layer_coarse = GetBlur2(uv);\n// Differential amplification\nret = abs(layer_fine * 2.0 + layer_med - layer_coarse * 0.6);\n// Musical noise coupling\nret *= lum(synaptic_noise) * (2.0 + neural_power * 1.0);\n// Contrast sculpting\nret = pow(ret, vec3(0.75, 0.70, 0.65));\nret = ret * (1.0 + (0.5 - ret) * 0.2);\n// Neural inversion\nfloat invert_amount = 0.5 + neural_power * 0.2;\nret = mix(ret, 1.0 - ret, invert_amount);\n// Chroma shifting\nret.r *= 1.02;\nret.g *= 0.98;\nret.b *= 1.05;\nret = clamp01(ret);\n}"
          },
          "warnings": [],
          "errors": [],
          "diagnostics": {
            "lineCount": 213,
            "sourceLength": 8803,
            "issueCount": 1,
            "issues": [
              {
                "severity": "warning",
                "code": "noise-volume-sampler",
                "message": "References Milkwave volume-noise samplers that are not currently bound by the runtime."
              }
            ]
          }
        }
      }
    }
  }
}