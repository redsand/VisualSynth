{
  "version": 6,
  "metadata": {
    "version": 6,
    "name": "Claude4 01",
    "author": "IBLS",
    "source": "IBLS - Claude4 01.milk",
    "importedFrom": "Milkwave",
    "createdAt": "2026-03-20T00:49:35.198Z",
    "updatedAt": "2026-03-20T00:49:35.198Z",
    "category": "Audio Reactive",
    "compatibility": {
      "minVersion": "1.4.0"
    },
    "milkwave": {
      "format": "milkwave-ir",
      "version": 1,
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-warp",
        "custom-comp",
        "preset-frame",
        "preset-pixel"
      ],
      "reasons": [],
      "translation": {
        "pipeline": "milkwave-offline-v1",
        "runtimePatchRecommended": true,
        "generatedPasses": [
          "warp",
          "comp"
        ]
      }
    },
    "activeEngineId": "engine-radial-core",
    "activeModeId": "mode-cosmic",
    "intendedMusicStyle": "Electronic",
    "visualIntentTags": [
      "imported",
      "milkwave"
    ],
    "colorChemistry": [
      "analog",
      "balanced"
    ],
    "defaultTransition": {
      "durationMs": 600,
      "curve": "easeInOut"
    }
  },
  "scenes": [
    {
      "id": "scene-1",
      "scene_id": "scene-1",
      "name": "Main",
      "intent": "ambient",
      "duration": 0,
      "transition_in": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "transition_out": {
        "durationMs": 600,
        "curve": "easeInOut"
      },
      "trigger": {
        "type": "manual"
      },
      "assigned_layers": {
        "core": [],
        "support": [
          "layer-milkwave"
        ],
        "atmosphere": []
      },
      "layers": [
        {
          "id": "layer-milkwave",
          "name": "Milkwave",
          "role": "support",
          "enabled": true,
          "opacity": 1,
          "blendMode": "screen",
          "transform": {
            "x": 0,
            "y": 0,
            "scale": 1,
            "rotation": 0
          },
          "params": {
            "opacity": 1,
            "enabled": true,
            "blendMode": "screen",
            "transform": {
              "x": 0,
              "y": 0,
              "scale": 1,
              "rotation": 0
            },
            "zoom": 1,
            "rotation": 0,
            "warp": 0,
            "decay": 0.95,
            "gamma": 1,
            "bassSensitivity": 1,
            "midSensitivity": 1,
            "trebSensitivity": 1
          }
        }
      ]
    }
  ],
  "activeSceneId": "scene-1",
  "roleWeights": {
    "core": 1,
    "support": 1,
    "atmosphere": 1
  },
  "tempoSync": {
    "bpm": 120,
    "source": "manual"
  },
  "modulations": [],
  "macros": [],
  "_shaderData": {
    "warp": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nvec2 laser_center1 = vec2(0.2 + sin(uTime*1.3)*0.1, 0.3 + cos(uTime*0.9)*0.1);\nvec2 laser_center2 = vec2(0.8 + sin(uTime*1.7)*0.1, 0.7 + cos(uTime*1.1)*0.1);\nvec2 laser_center3 = vec2(0.5 + sin(uTime*2.1)*0.15, 0.2 + cos(uTime*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + uTime * 0.5 + audioLowSmooth * 2.0;\nvec2 direction = vec2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, vec2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100.0 + 1)) * (1.0 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, vec2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80.0 + 1)) * (1.0 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = uTime * 1.5 + audioHighSmooth * 3.0;\nvec2 fan_center = vec2(0.5, 0.8);\nvec2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50.0 + 1)) * (1.0 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nvec2 circle_center1 = vec2(0.3 + sin(uTime*2.3)*0.2, 0.6 + cos(uTime*1.8)*0.15);\nvec2 circle_center2 = vec2(0.7 + sin(uTime*1.9)*0.15, 0.4 + cos(uTime*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + audioLowSmooth * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200.0 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200.0 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(uTime * 3.0 + audioMidSmooth * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(uTime*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(uTime*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(uTime*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nvec2 laser_center1 = vec2(0.2 + sin(uTime*1.3)*0.1, 0.3 + cos(uTime*0.9)*0.1);\nvec2 laser_center2 = vec2(0.8 + sin(uTime*1.7)*0.1, 0.7 + cos(uTime*1.1)*0.1);\nvec2 laser_center3 = vec2(0.5 + sin(uTime*2.1)*0.15, 0.2 + cos(uTime*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + uTime * 0.5 + audioLowSmooth * 2.0;\nvec2 direction = vec2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, vec2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100.0 + 1)) * (1.0 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, vec2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80.0 + 1)) * (1.0 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = uTime * 1.5 + audioHighSmooth * 3.0;\nvec2 fan_center = vec2(0.5, 0.8);\nvec2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50.0 + 1)) * (1.0 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nvec2 circle_center1 = vec2(0.3 + sin(uTime*2.3)*0.2, 0.6 + cos(uTime*1.8)*0.15);\nvec2 circle_center2 = vec2(0.7 + sin(uTime*1.9)*0.15, 0.4 + cos(uTime*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + audioLowSmooth * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200.0 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200.0 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(uTime * 3.0 + audioMidSmooth * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(uTime*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(uTime*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(uTime*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}\n  fragColor = vec4(ret, 1.0);\n}",
    "comp": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\n// Nachbearbeitung für Glow-Effekt\nvec3 color = texture(sampler_main, uv).xyz;\n// Bloom-Effekt für Laser-Glow\nvec3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nvec2 offset = vec2(i, j) * (vec2(1.0)/uTexSize) * 2.0;\nbloom += texture(sampler_main, uv + offset).xyz;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = vec4(color, 1.0 );\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\n// Nachbearbeitung für Glow-Effekt\nvec3 color = texture(sampler_main, uv).xyz;\n// Bloom-Effekt für Laser-Glow\nvec3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nvec2 offset = vec2(i, j) * (vec2(1.0)/uTexSize) * 2.0;\nbloom += texture(sampler_main, uv + offset).xyz;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = vec4(color, 1.0 );\n}\n  fragColor = vec4(ret, 1.0);\n}",
    "perFrameCode": [
      "bass_att = bass_att*0.9 + bass*0.1;",
      "mid_att = mid_att*0.9 + mid*0.1;",
      "treb_att = treb_att*0.9 + treb*0.1;",
      "",
      "laser_time = laser_time + 0.02;",
      "beat_strength = (bass + mid + treb) * 0.33;",
      "",
      "// Schwarzer Hintergrund für Lasershow-Effekt",
      "decay = 0.98 - beat_strength*0.02;",
      "",
      "// Dynamische Rotation basierend auf Bass",
      "rot = rot + bass_att*0.05;",
      "",
      "// Zoom-Pulsation",
      "zoom = 1 + bass_att*0.1 + sin(time*2)*0.05;",
      "q1 = beat_strength;"
    ],
    "perFrameInitCode": [],
    "perPixelCode": [
      "// Schwarzer Hintergrund",
      "r = 0;",
      "g = 0;",
      "b = 0;",
      "a = 1;"
    ],
    "waves": [],
    "shapes": [],
    "originalParameters": {
      "fRating": 3,
      "fGammaAdj": 1,
      "fDecay": 0.95,
      "fVideoEchoZoom": 1,
      "fVideoEchoAlpha": 0,
      "nVideoEchoOrientation": false,
      "nWaveMode": false,
      "bAdditiveWaves": true,
      "bWaveDots": true,
      "bWaveThick": true,
      "bModWaveAlphaByVolume": true,
      "bMaximizeWaveColor": false,
      "bTexWrap": false,
      "bDarkenCenter": false,
      "bRedBlueStereo": false,
      "bBrighten": false,
      "bDarken": false,
      "bSolarize": false,
      "bInvert": false,
      "fWaveAlpha": 1,
      "fWaveScale": 1,
      "fWaveSmoothing": 0.75,
      "fWaveParam": 0,
      "fModWaveAlphaStart": 0.75,
      "fModWaveAlphaEnd": 0.95,
      "fWarpAnimSpeed": 1,
      "fWarpScale": 1,
      "fZoomExponent": 1,
      "fShader": 0,
      "zoom": 1,
      "rot": 0,
      "cx": 0.5,
      "cy": 0.5,
      "dx": 0,
      "dy": 0,
      "warp": 0,
      "sx": 1,
      "sy": 1,
      "wave_r": 1,
      "wave_g": 0,
      "wave_b": 0,
      "wave_x": 0.5,
      "wave_y": 0.5,
      "ob_size": 0,
      "ob_r": 0,
      "ob_g": 0,
      "ob_b": 0,
      "ob_a": 0,
      "ib_size": 0,
      "ib_r": 0,
      "ib_g": 0,
      "ib_b": 0,
      "ib_a": 0,
      "nMotionVectorsX": false,
      "nMotionVectorsY": false,
      "mv_dx": 0,
      "mv_dy": 0,
      "mv_l": 1,
      "mv_r": 1,
      "mv_g": 1,
      "mv_b": 1,
      "mv_a": 0,
      "b1n": false,
      "b2n": false,
      "b3n": false,
      "b1x": false,
      "b2x": false,
      "b3x": false,
      "b1ed": false
    },
    "translation": {
      "pipeline": "milkwave-offline-v1",
      "supportTier": "native-supported",
      "featureSummary": [
        "custom-warp",
        "custom-comp",
        "preset-frame",
        "preset-pixel"
      ],
      "runtimePatchRecommended": true,
      "passes": {
        "warp": {
          "kind": "warp",
          "requested": true,
          "generated": true,
          "source": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nvec2 laser_center1 = vec2(0.2 + sin(uTime*1.3)*0.1, 0.3 + cos(uTime*0.9)*0.1);\nvec2 laser_center2 = vec2(0.8 + sin(uTime*1.7)*0.1, 0.7 + cos(uTime*1.1)*0.1);\nvec2 laser_center3 = vec2(0.5 + sin(uTime*2.1)*0.15, 0.2 + cos(uTime*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + uTime * 0.5 + audioLowSmooth * 2.0;\nvec2 direction = vec2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, vec2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100.0 + 1)) * (1.0 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, vec2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80.0 + 1)) * (1.0 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = uTime * 1.5 + audioHighSmooth * 3.0;\nvec2 fan_center = vec2(0.5, 0.8);\nvec2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50.0 + 1)) * (1.0 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nvec2 circle_center1 = vec2(0.3 + sin(uTime*2.3)*0.2, 0.6 + cos(uTime*1.8)*0.15);\nvec2 circle_center2 = vec2(0.7 + sin(uTime*1.9)*0.15, 0.4 + cos(uTime*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + audioLowSmooth * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200.0 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200.0 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(uTime * 3.0 + audioMidSmooth * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(uTime*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(uTime*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(uTime*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nvec2 laser_center1 = vec2(0.2 + sin(uTime*1.3)*0.1, 0.3 + cos(uTime*0.9)*0.1);\nvec2 laser_center2 = vec2(0.8 + sin(uTime*1.7)*0.1, 0.7 + cos(uTime*1.1)*0.1);\nvec2 laser_center3 = vec2(0.5 + sin(uTime*2.1)*0.15, 0.2 + cos(uTime*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + uTime * 0.5 + audioLowSmooth * 2.0;\nvec2 direction = vec2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, vec2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100.0 + 1)) * (1.0 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, vec2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80.0 + 1)) * (1.0 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = uTime * 1.5 + audioHighSmooth * 3.0;\nvec2 fan_center = vec2(0.5, 0.8);\nvec2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50.0 + 1)) * (1.0 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nvec2 circle_center1 = vec2(0.3 + sin(uTime*2.3)*0.2, 0.6 + cos(uTime*1.8)*0.15);\nvec2 circle_center2 = vec2(0.7 + sin(uTime*1.9)*0.15, 0.4 + cos(uTime*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + audioLowSmooth * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200.0 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200.0 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(uTime * 3.0 + audioMidSmooth * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(uTime*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(uTime*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(uTime*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}\n  fragColor = vec4(ret, 1.0);\n}",
          "backend": "milkwave-direct-v2",
          "normalized": {
            "preludeLineCount": 69,
            "bodyLineCount": 0,
            "helperLineCount": 0,
            "hasShaderBodyBlock": false,
            "source": "shader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nfloat2 laser_center1 = float2(0.2 + sin(time*1.3)*0.1, 0.3 + cos(time*0.9)*0.1);\nfloat2 laser_center2 = float2(0.8 + sin(time*1.7)*0.1, 0.7 + cos(time*1.1)*0.1);\nfloat2 laser_center3 = float2(0.5 + sin(time*2.1)*0.15, 0.2 + cos(time*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + time * 0.5 + bass_att * 2.0;\nfloat2 direction = float2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, float2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100 + 1)) * (1 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, float2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80 + 1)) * (1 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = time * 1.5 + treb_att * 3.0;\nfloat2 fan_center = float2(0.5, 0.8);\nfloat2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan2(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50 + 1)) * (1 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nfloat2 circle_center1 = float2(0.3 + sin(time*2.3)*0.2, 0.6 + cos(time*1.8)*0.15);\nfloat2 circle_center2 = float2(0.7 + sin(time*1.9)*0.15, 0.4 + cos(time*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + bass_att * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(time * 3.0 + mid_att * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(time*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(time*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(time*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}",
            "dialectSource": "shader_body\n{\nuv = uv_orig;\n// Laser-Strahlen von verschiedenen Punkten\nfloat laser_intensity = 0;\n// Zentrale Laserpunkte\nvec2 laser_center1 = vec2(0.2 + sin(uTime*1.3)*0.1, 0.3 + cos(uTime*0.9)*0.1);\nvec2 laser_center2 = vec2(0.8 + sin(uTime*1.7)*0.1, 0.7 + cos(uTime*1.1)*0.1);\nvec2 laser_center3 = vec2(0.5 + sin(uTime*2.1)*0.15, 0.2 + cos(uTime*1.5)*0.1);\n// Strahleffekte zu verschiedenen Punkten\nfor(int i = 0; i < 8; i++)\n{\nfloat angle = (float)i * 0.785 + uTime * 0.5 + audioLowSmooth * 2.0;\nvec2 direction = vec2(cos(angle), sin(angle));\n// Strahl von Zentrum 1\nfloat beam_dist1 = abs(dot(uv - laser_center1, vec2(-direction.y, direction.x)));\nfloat beam_length1 = dot(uv - laser_center1, direction);\nif(beam_length1 > 0 && beam_length1 < 1.5)\n{\nlaser_intensity += (1.0 / (beam_dist1 * 100.0 + 1)) * (1.0 - beam_length1/1.5);\n}\n// Strahl von Zentrum 2\nfloat beam_dist2 = abs(dot(uv - laser_center2, vec2(-direction.y, direction.x)));\nfloat beam_length2 = dot(uv - laser_center2, direction);\nif(beam_length2 > 0 && beam_length2 < 1.2)\n{\nlaser_intensity += (1.0 / (beam_dist2 * 80.0 + 1)) * (1.0 - beam_length2/1.2);\n}\n}\n// Rotierende Fächer\nfloat fan_angle = uTime * 1.5 + audioHighSmooth * 3.0;\nvec2 fan_center = vec2(0.5, 0.8);\nvec2 fan_dir = uv - fan_center;\nfloat fan_dist = length(fan_dir);\nfloat fan_angle_pos = atan(fan_dir.y, fan_dir.x);\n// Mehrere Fächerstrahlen\nfor(int j = 0; j < 5; j++)\n{\nfloat sector_angle = fan_angle + (float)j * 1.256;\nfloat angle_diff = abs(fan_angle_pos - sector_angle);\nangle_diff = min(angle_diff, 6.283 - angle_diff);\nif(angle_diff < 0.1 && fan_dist < 0.8)\n{\nlaser_intensity += (1.0 / (angle_diff * 50.0 + 1)) * (1.0 - fan_dist/0.8);\n}\n}\n// Kreisförmige Laserpunkte\nvec2 circle_center1 = vec2(0.3 + sin(uTime*2.3)*0.2, 0.6 + cos(uTime*1.8)*0.15);\nvec2 circle_center2 = vec2(0.7 + sin(uTime*1.9)*0.15, 0.4 + cos(uTime*2.2)*0.2);\nfloat circle_dist1 = length(uv - circle_center1);\nfloat circle_dist2 = length(uv - circle_center2);\n// Pulsende Kreise\nfloat circle_pulse = 0.02 + audioLowSmooth * 0.01;\nif(abs(circle_dist1 - 0.1) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist1 - 0.1) * 200.0 + 1);\nif(abs(circle_dist2 - 0.08) < circle_pulse)\nlaser_intensity += 2.0 / (abs(circle_dist2 - 0.08) * 200.0 + 1);\n// Scanning-Linien\nfloat scan_y = 0.5 + sin(uTime * 3.0 + audioMidSmooth * 2.0) * 0.4;\nfloat scan_dist = abs(uv.y - scan_y);\nif(scan_dist < 0.002)\nlaser_intensity += 1.5;\n// Farbgebung für verschiedene Laser-Typen\nret.r = laser_intensity * (0.8 + sin(uTime*3.0)*0.2);  // Rot\nret.g = laser_intensity * 0.3 * (0.5 + cos(uTime*2.5)*0.5);  // Wenig Grün\nret.b = laser_intensity * (0.6 + sin(uTime*4.0)*0.4);  // Blau\n// Intensitätsverstärkung basierend auf Musik\nret.rgb *= (1.0 + q1 * 2.0);\n}"
          },
          "warnings": [],
          "errors": [],
          "diagnostics": {
            "lineCount": 251,
            "sourceLength": 9955,
            "issueCount": 1,
            "issues": [
              {
                "severity": "warning",
                "code": "noise-volume-sampler",
                "message": "References Milkwave volume-noise samplers that are not currently bound by the runtime."
              }
            ]
          }
        },
        "comp": {
          "kind": "comp",
          "requested": true,
          "generated": true,
          "source": "#version 300 es\nprecision highp float;\n\n// VisualSynth uniforms\nuniform float uTime;\nuniform float uFrame;\nuniform float uFps;\nuniform float uRms;\n\n// Audio uniforms\nuniform float audioLow;\nuniform float audioLowSmooth;\nuniform float audioMid;\nuniform float audioMidSmooth;\nuniform float audioHigh;\nuniform float audioHighSmooth;\n\n// Screen uniforms\nuniform float uAspectX;\nuniform float uAspectY;\nuniform vec2 uAspect;\nuniform vec2 uTexSize;\n#define texsize vec4(uTexSize, 1.0/uTexSize)\n\n// Random values\nuniform vec2 uRandomPreset;\nuniform vec2 uRandomFrame;\nuniform vec4 rand_preset;\nuniform vec4 rand_frame;\n\n// Input from vertex shader\nin vec2 vUv;\nin vec2 vUvOriginal;\nin float vRadius;\nin float vAngle;\n\n// Output\nout vec4 fragColor;\n\n// Textures\nuniform sampler2D sampler_main;\nuniform sampler2D sampler_noise_lq;\nuniform sampler2D sampler_noise_mq;\nuniform sampler2D sampler_noise_hq;\nuniform sampler2D sampler_blur1;\nuniform sampler2D sampler_blur2;\nuniform sampler2D sampler_blur3;\n#define sampler_fc_main sampler_main\n#define sampler_pc_main sampler_main\n#define sampler_fw_main sampler_main\n#define sampler_pw_main sampler_main\n#define sampler_noise_lq_lite sampler_noise_lq\n#define sampler_noisevol_lq sampler_noise_lq\n#define sampler_noisevol_hq sampler_noise_hq\n\n// MilkDrop q-variable uniforms (q1–q32)\nuniform float q1; uniform float q2; uniform float q3; uniform float q4;\nuniform float q5; uniform float q6; uniform float q7; uniform float q8;\nuniform float q9; uniform float q10; uniform float q11; uniform float q12;\nuniform float q13; uniform float q14; uniform float q15; uniform float q16;\nuniform float q17; uniform float q18; uniform float q19; uniform float q20;\nuniform float q21; uniform float q22; uniform float q23; uniform float q24;\nuniform float q25; uniform float q26; uniform float q27; uniform float q28;\nuniform float q29; uniform float q30; uniform float q31; uniform float q32;\n\n// MilkDrop _c / _q matrix uniforms\nuniform vec4 _c0; uniform vec4 _c1; uniform vec4 _c2; uniform vec4 _c3;\nuniform vec4 _c4; uniform vec4 _c5; uniform vec4 _c6; uniform vec4 _c7;\nuniform vec4 _c8; uniform vec4 _c9; uniform vec4 _c10; uniform vec4 _c11;\nuniform vec4 _c12; uniform vec4 _c13; uniform vec4 _c14;\nuniform vec4 _c15; uniform vec4 _c16; uniform vec4 _c17;\nuniform vec4 _qa; uniform vec4 _qb; uniform vec4 _qc; uniform vec4 _qd;\nuniform vec4 _qe; uniform vec4 _qf; uniform vec4 _qg; uniform vec4 _qh;\n\n// MilkDrop rotation matrices\nuniform mat4 rot_s1; uniform mat4 rot_s2; uniform mat4 rot_s3; uniform mat4 rot_s4;\nuniform mat4 rot_d1; uniform mat4 rot_d2; uniform mat4 rot_d3; uniform mat4 rot_d4;\nuniform mat4 rot_f1; uniform mat4 rot_f2; uniform mat4 rot_f3; uniform mat4 rot_f4;\nuniform mat4 rot_vf1; uniform mat4 rot_vf2; uniform mat4 rot_vf3; uniform mat4 rot_vf4;\nuniform mat4 rot_uf1; uniform mat4 rot_uf2; uniform mat4 rot_uf3; uniform mat4 rot_uf4;\nuniform mat4 rot_rand1; uniform mat4 rot_rand2; uniform mat4 rot_rand3; uniform mat4 rot_rand4;\n\nfloat clamp01(float x) { return clamp(x, 0.0, 1.0); }\nvec2 clamp01(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 clamp01(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 clamp01(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\nfloat lum(vec3 x) { return dot(x, vec3(0.32, 0.49, 0.29)); }\nfloat lum(vec4 x) { return dot(x.rgb, vec3(0.32, 0.49, 0.29)); }\nfloat sat(float x) { return clamp(x, 0.0, 1.0); }\nvec2 sat(vec2 x) { return clamp(x, vec2(0.0), vec2(1.0)); }\nvec3 sat(vec3 x) { return clamp(x, vec3(0.0), vec3(1.0)); }\nvec4 sat(vec4 x) { return clamp(x, vec4(0.0), vec4(1.0)); }\n\nvec3 GetPixel(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec3 GetBlur1(vec2 uv) { return texture(sampler_blur1, uv).xyz; }\nvec3 GetBlur2(vec2 uv) { return texture(sampler_blur2, uv).xyz; }\nvec3 GetBlur3(vec2 uv) { return texture(sampler_blur3, uv).xyz; }\nvec4 noise3(vec2 uv) { return texture(sampler_noise_lq, uv); }\nvec3 GetMain(vec2 uv) { return texture(sampler_main, uv).xyz; }\nvec2 multiply(vec2 v, mat2 m) { return m * v; }\nvec3 multiply(vec3 v, mat3 m) { return m * v; }\nvec4 multiply(vec4 v, mat4 m) { return m * v; }\nvec4 textureBias(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nvec4 textureLod4(sampler2D s, vec4 uv4) { return textureLod(s, uv4.xy, uv4.w); }\nshader_body\n{\n// Nachbearbeitung für Glow-Effekt\nvec3 color = texture(sampler_main, uv).xyz;\n// Bloom-Effekt für Laser-Glow\nvec3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nvec2 offset = vec2(i, j) * (vec2(1.0)/uTexSize) * 2.0;\nbloom += texture(sampler_main, uv + offset).xyz;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = vec4(color, 1.0 );\n}\n\nvoid main() {\n  vec3 ret = vec3(0.0);\n  vec2 uv = vUv;\n  vec2 uv_orig = vUvOriginal;\n  float rad = vRadius;\n  float ang = vAngle;\n  shader_body\n{\n// Nachbearbeitung für Glow-Effekt\nvec3 color = texture(sampler_main, uv).xyz;\n// Bloom-Effekt für Laser-Glow\nvec3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nvec2 offset = vec2(i, j) * (vec2(1.0)/uTexSize) * 2.0;\nbloom += texture(sampler_main, uv + offset).xyz;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = vec4(color, 1.0 );\n}\n  fragColor = vec4(ret, 1.0);\n}",
          "backend": "milkwave-direct-v2",
          "normalized": {
            "preludeLineCount": 21,
            "bodyLineCount": 0,
            "helperLineCount": 0,
            "hasShaderBodyBlock": false,
            "source": "shader_body\n{\n// Nachbearbeitung für Glow-Effekt\nfloat3 color = tex2D(sampler_main, uv).rgb;\n// Bloom-Effekt für Laser-Glow\nfloat3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nfloat2 offset = float2(i, j) * texsize.zw * 2.0;\nbloom += tex2D(sampler_main, uv + offset).rgb;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = float4(color, 1);\n}",
            "dialectSource": "shader_body\n{\n// Nachbearbeitung für Glow-Effekt\nvec3 color = texture(sampler_main, uv).xyz;\n// Bloom-Effekt für Laser-Glow\nvec3 bloom = 0;\nfor(int i = -2; i <= 2; i++)\n{\nfor(int j = -2; j <= 2; j++)\n{\nvec2 offset = vec2(i, j) * (vec2(1.0)/uTexSize) * 2.0;\nbloom += texture(sampler_main, uv + offset).xyz;\n}\n}\nbloom /= 25.0;\n// Glow hinzufügen\ncolor += bloom * 0.5;\n// Kontrast erhöhen für schärfere Laser\ncolor = pow(color, 0.8);\nret = vec4(color, 1.0 );\n}"
          },
          "warnings": [],
          "errors": [],
          "diagnostics": {
            "lineCount": 155,
            "sourceLength": 5367,
            "issueCount": 1,
            "issues": [
              {
                "severity": "warning",
                "code": "noise-volume-sampler",
                "message": "References Milkwave volume-noise samplers that are not currently bound by the runtime."
              }
            ]
          }
        }
      }
    }
  }
}